version: 3

dotenv:
  - .env
tasks:
  run:
    cmds:
      - go run ./cmd/server
    silent: true

  test:
    cmds:
      - go test ./... -v
    silent: true

  docs:
    cmds:
      - swag fmt
      - swag init --dir ./internal/api --generalInfo api.go --parseInternal --parseDependency
    silent: true

  sql:
    cmds:
      - sqlc generate
    silent: true

  datagen:
    cmds:
      - go run ./cmd/datagen
    silent: true
